{
	"build": {
		"dockerfile": "./Dockerfile",
		"context": "."
	},
	// NOTE: remove this if undesired
	"runArgs": ["--network=host"],

	// TODO: mount dotnet cache (and other caches)

	"customizations": {
		"vscode": {
			"extensions": [
				"ms-dotnettools.csdevkit",
				"ms-dotnettools.csharp",
				"ms-dotnettools.vscodeintellicode-csharp"
			]
		}
	}
}
